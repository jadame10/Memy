import { atom } from "recoil";

export const hot = atom({
    key: "hot",
    default: []
   
  });
  export const regular = atom({
    key: "regular",
    default:  [],
   
  });
  export const result = atom({
    key: "result",
    default:  [],
   
  });